kaka58/
  src/
  public/
  package.json
  ...
```

이렇게 하면 터미널을 열었을 때 바로 `kaka58` 폴더에서 `npm run dev`를 실행할 수 있습니다!

**진행 방법:**
1. 현재 실행 중인 개발 서버를 먼저 종료
2. `law-firm-website` 폴더 안의 모든 파일을 `kaka58` 폴더로 이동
3. 빈 `law-firm-website` 폴더 삭제

진행해도 될까요? 먼저 현재 실행 중인 서버를 종료하겠습니다: 